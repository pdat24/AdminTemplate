interface props {
    text: string;
    iconFont?: string;
    // textFont?: string;
}
function NoSelectedChat({ text, iconFont }: props) {
    return (
        
    );
}

export default NoSelectedChat;
